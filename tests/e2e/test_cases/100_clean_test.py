# clean up all product data
# clean up all category data
# clean up all order data
# clean up all cart data
# clean up all user data


# from logging import Logger

# def test_cleanup_user_data(api_request_context: APIRequestContext, logger: Logger):
#     response = delete_user(api_request_context, USER_NORMAL["id"])
#     assert response.status == 204, f"Expected 204 No Content, got {response.status}"
#     logger.info("User data cleaned up successfully.")
